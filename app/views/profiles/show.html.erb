<div class="flex">
  <div class="ml-auto mr-auto p-10">
    <h1 class="text-3xl">Profile.</h1>
    <%= render @profile %>
    <br>
    <div>
      <%= link_to "Edit profile", edit_profile_path(@profile), class:"bg-green-700 text-white font-semibold py-2 px-5 text-sm mt-6 inline-flex items-center group" %> | 
      <%= link_to "Edit account", edit_registration_path(current_user), class:"bg-blue-700 text-white font-semibold py-2 px-5 text-sm mt-6 inline-flex items-center group" %>
    </div>
  </div>
</div>